/var

var parkWords = [
    "sequoia", "glacier", "olympic", "redwood", "saguaro"
];

var select = [Math.floor(Math.random() * 5)];
var currentWord = parkWords[select];
var myLength = currentWord.length;

//create variables that hold references to the places in HTML where we want to display things
var displayText = document.getElementById("display");
var wrongText = document.getElementById("wrong-display");
var livesText = document.getElementById("lives");
var winsText = document.getElementById("wins-display");
var lossesText = document.getElementById("losses-display");

var gameSetup = [
    lives = 7,
    guessedLetters = [],
    lettersMatched = [],
    wins = 0,
    losses = 0,
];

//select random word

var getWord = function () {
    return [Math.floor(Math.random() * parkWords.length)];

}

var currentWord = getWord(parkWords);



//track user input
document.onkeyup = function (event) {
        var guess = event.key;

        document.getElementById("submitguess").onclick = function () {

            document.getElementById("guessField").value = guess;

            if (guess == (currentWord.indexOf) > -1) {
                lettersMatched.push("");  
                wins++;
            }
        }
        }




        //compare user guess
        function guessed() {
            for (var i = 0; i < 7; i++) {
                if (userGuess.indexOf(currentWord) > -1) {
                    console.log(i);
                }
            }
                }

        //input values

    